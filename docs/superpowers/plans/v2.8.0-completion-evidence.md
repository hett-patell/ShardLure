# v2.8.0 completion evidence

Approved execution: Native, with independent whole-branch review after implementation.
Plan: [v2.8.0 completion](2026-09-21-v2.8.0-completion.md).
Spec: [approved design](../specs/2026-09-21-reliability-completion-design.md).
Starting commit: e81b86e. No production changes are part of development tests.

Tasks are not complete until their regressions and verification have run. The
plan-scoped scratch ledger retains command output and resumable execution state.

## Decisions

- Execute Task 6 before Task 5, as required by their declared safe-file dependency.

## Tasks 15–16 — integrated installer candidate (September 23; acceptance pending)

- Confirmed REDs: literal YAML paths; failed-sync ownership publication;
  duplicate UIDs; unproven non-purging uninstall; pathname permission/symlink
  races; unvalidated unit activation; directory inode reuse and nested mounts;
  SSH restore validation/activation failures, missing login verification and
  operator Match context; authorized_keys symlink writes.
- The shared stdlib-only safety helper now protects metadata writes and
  administrative-resource ownership, binds provenance to a private data stamp,
  refuses active/unrecognized installations, and preserves customized resources.
  Cowrie/source adoption no longer reapplies a persona or root pip upgrade.
- SSHTransition records original bytes/modes/ownership, listeners and operator
  context, stages dual access, checks -t and -T -C, requires separate authenticated
  logins, checks activation results, and retains recovery records on failure and
  success. Public-key updates use scoped descriptors rather than recursive chown.
- All 117 local Python tests passed. Full Go suite passed (store 100.521s,
  web 29.179s); vet, Python compilation, shell syntax and diff checks passed.
  These are local fixture results, not a real systemd/SSH acceptance claim.
- Added a reusable disposable Ubuntu workflow consuming the calling run's native
  artifact. Release publication depends on this job and cross-build. The harness
  rejects unmarked/normal/self-hosted machines and uploads only a redacted receipt.
  **Actual hosted-guest run is pending; both tasks remain open.**
- Deliberate constraints: actual filesystem actions require Python 3 even in
  no-Cowrie mode (no extra Python packages); release executable inputs must be
  checksummed; account/authbind cleanup remains manual; unfamiliar legacy/custom
  ownership and nested mounts require review. See INSTALLER-INTEGRATION.md.

## Task 1 — exact event aggregates

- Actual-method regressions reproduced whole/fractional cutoff omissions,
  mixed-offset hour/tunnel errors, wrong recent/actor/capture command ordering,
  nullable legacy detail failures, and malformed timestamp acceptance.
- Credential, activity, hourly, tunnel, and command readers now use exact
  native/legacy branches; ordering ties use the event ID. Legacy parsing errors
  do not disclose the malformed value.
- Query-plan/allocation tests exposed a broad kind-index legacy scan. Separate
  global/kind-filtered legacy branches retain the shrinking partial index while
  actor/session/source reads retain their scoped indexes.
- Allocation regression: migrated 10 -> 3,010 events costs 79 -> 83 allocations;
  mixed history costs 103 -> 107, with the exact 3,010 event count preserved.
- Full Go suite PASS after the final change (store 67.513s, web 22.275s), vet
  PASS, and focused race tests PASS (38.347s). Final post-commit task verification
  is recorded by the plan-scoped task-done script: cfa44ad; store 69.976s,
  web 23.656s. Task 1 complete.

Rulings:

- Include recent/per-actor/batched last-command readers in intel.go because the
  same ordering defect was reproduced there; cost is additional read regression
  scope, with no ingestion or historical data rewrite.
- Separate global legacy-index selection from scoped selection; cost if wrong
  is query-plan regression, guarded by allocation and actor-index tests.
- Align distinct password counts with password lists for literal question-mark
  passwords. The regression proved an exported row with a total of zero. Unknown
  usernames remain excluded; reporting eligibility is unchanged.

## Task 2 — submission-ledger exact ordering

- Real Record/List/Stats regressions reproduced older-first order and wrong
  latest instants in Bazaar, URLhaus, and ThreatFox. Malformed history could
  silently disappear behind LIMIT/MAX. The joined Bazaar history was affected.
- Additive v22 creates nullable canonical keys and native/legacy indexes in one
  schema transaction. Startup preserves original timestamps, statuses, provider
  metadata, and dedup rows; bounded background repair populates only keys.
- Repair uses one atomic transaction for reads, writes, and per-table cursors,
  at most 1,000 rows per batch. Malformed rows remain recorded and visible as
  sanitized read errors, while repair advances to later rows. Cancellation and
  failed transactions report no committed progress.
- Focused tests pass for all fraction widths, offsets, nanosecond differences,
  deterministic ties, populated pre-v22 reopen, optional tables, partial
  repair/restart, rollback, invalid-then-corrected rows, concurrent writes,
  native-key validation, and startup backfill orchestration.
- EXPLAIN confirms native LIMIT/latest index paths plus the shrinking legacy
  index. List/stats allocations for 10 -> 3,010 migrated records: Bazaar
  68 -> 70, URLhaus 80 -> 82, ThreatFox 89 -> 91.
- Final full Go suite PASS (store 76.988s, web 23.281s), focused race PASS
  (store 25.502s, CLI 1.647s), and vet PASS. The first whole-suite run exposed
  the inconsistent v20 fixture described below; its corrected regression and
  the subsequent full suite pass. Post-commit verification PASS at a98ff64:
  store 78.437s, web 24.329s, CLI 3.260s. Task 2 complete.

Rulings:

- Standard-SQL triggers invalidate stale keys and rewind repair cursors when an
  old writer updates a timestamp without updating its key. Cost if wrong is an
  extra bounded repair pass; original ledger and dedup state stay intact.
- Require strict RFC3339 with at most nine fractional digits. Tests reproduced
  Go silently truncating extra precision and accepting invalid timezone fields.
  Cost is a sanitized error for unsupported historical syntax instead of an
  invented instant; the raw value is preserved.
- Adjust the v20 upgrade test fixture to remove all migration stamps >=21.
  Leaving v22 in a simulated v20 database suppressed the migration under test.
  This is a fixture-only correction; no-rewrite/index assertions are retained.

## Task 3 — journal profiles and annotation ownership

- Reproduced and fixed annotation loss through live append/replay/reopen, batch
  replacement/upsert, and reconciliation of an orphan whose note resembled old
  generated text. Classifier summaries now have a separate generated-notes field.
- Shared, versioned length framing distinguishes comma-containing username sets
  in batch, durable, and fresh per-IP reporting fingerprints. Unicode and a real
  `_overflow_` username remain actual identities, never cache sentinels.
- Additive v23 provides revision-fenced, bounded journal summary pages and a
  resumable SHA-256/classifier fold. Late names restart the sorted fold; corrupt
  state resets without deleting usernames. Completed folds can be reused when
  only event counters/rates change. Worker scheduling belongs to Task 12.
- Live hydration reads counters only: the old reader reproduced 75 -> 30,123
  allocations with 6,000 names; the new reader stays 31 -> 31. Cache overflow is
  a scalar, not a synthetic name. An 8 MiB corrupt fold reproduced a 16,841,616
  byte allocation; SQL-side validation prevents loading it into Go.
- Real batch ingestion reproduced 100 historical attempts and usernames being
  replaced by one fresh event. Bounded, post-dedup atomic appends now preserve
  lifetime counters/corpora and share the live append path.
- Missing/unproven historical coverage is explicitly unknown, not a fabricated
  fresh hash or classification. Stored legacy evidence and annotations remain
  intact. Actor responses and aggregate playbook distributions use the same
  state; fresh per-IP reporting still derives its own eligible evidence.
- Targeted checks pass for transaction rollback, cancellation, concurrent name
  insertion, partial pages/restart, malformed state, metadata preservation,
  allocation bounds, and API status fields. The first full/race runs identified
  a handwritten report-selection fixture that now explicitly models a completed
  profile; its original ranking assertions remain. Final full Go suite PASS
  (store 248.716s, web 23.990s), vet PASS, and full affected-package race PASS
  (actor 72.252s, store 354.722s, Cowrie 16.408s, journal 3.847s, web 113.923s).
  Post-commit task verification PASS at 857bfc7: actor 7.958s, store 81.269s,
  Cowrie 5.477s, journal 1.047s, web 25.228s. Task 3 complete.

Rulings:

- Include report_evidence.go in shared framing to retain builder/report parity;
  cost is a derived fingerprint change, not altered Vet policy or observations.
- Extend journal batch ingestion with bounded atomic append because the real
  retained-history regression proved lifetime loss. Cost is changed persistence
  mechanics, guarded by deduplication/counter/rollback tests.
- Keep pre-v23/unproven profiles unknown rather than certify retained subtotals.
  Cost: legacy profiles remain unverified until authoritative rebuilding is
  available; existing evidence remains stored and reporting uses fresh evidence.
- Bound summary pages by 1 MiB and 1,000 names, with an 8 KiB fold cap. Larger
  bypassed-parser names remain stored but unclassified. Cost is declining to
  classify extraordinary raw-DB input, rather than unbounded allocation.
- Include playbook aggregate reads in derivation-state handling. Cost is a
  changed dashboard distribution, not changed evidence or reporting eligibility.

## Task 4 — durable file-capture store contract

- The real old archiver reproduced only 200 archived files out of 406 inert
  download events. The baseline fixture and failure log are preserved in the
  plan workspace for the required Task 5 end-to-end integration regression.
- Additive v24 records per-event file jobs, provenance, normalized native
  basenames, bounded retries, persistent lease tokens, and terminal diagnostics.
  Discovery streams bounded primary-key/byte pages and commits each checkpoint
  with its work. It retains late timestamps and survives reopen without loss.
- Independent stores claim a job only once. Expired/replaced leases and observed
  clock rewinds cannot revive old tokens. Completion and per-event artifact
  association commit together; an existing quarantine-fetch row sharing the
  delivery URL is unchanged. Source protection uses a partial indexed lookup.
- File and command pages have a 4 MiB byte budget. Command fanout/work limits
  also bound transactions; malformed commands are durably diagnosed rather than
  stalling later discovery or echoing raw timestamps. Native filename bytes are
  preserved, including literal Unix backslashes.
- Hashless files can be archived without inventing remote-fetch freshness;
  their unknown freshness survives observation touches and timestamp backfill.
- Full Go suite PASS (store 77.160s, web 24.086s), vet PASS, and capture-focused
  race checks PASS over three runs (store 95.983s, capture 3.236s). Post-commit
  verification PASS at 05923c0: store 77.908s, capture 2.309s. Task 4 complete.
  Runner/filesystem integration remains Task 5 after 6.

Rulings:

- Preserve the failing old-runner fixture outside the active suite during this
  store-only task; Task 5 must turn its 406-event expectation green. Cost is an
  integration check to carry forward, not a claim that the runner is fixed yet.
- Add a terminal command-discovery diagnostic table to address the reproduced
  malformed-row stall. Task 5 retention and Task 11 diagnostics must include it.
  Cost is one additive metadata table, not fabricated successful capture.
- Limit command fanout to 512 URLs/2 MiB of URL text and approximately 2,000 URL
  operations per page, finishing the current bounded command. Cost is explicit
  rejection of extreme fanout; original events follow ordinary retention.
- Persist a clock floor and invalidate the matching token on observed expiry or
  rewind. Cost is delayed retry during a large clock correction, never stale
  completion acceptance.
- Preserve unknown remote-fetch freshness when no original expected hash exists.
  Cost is withholding sharing eligibility for unverified provenance while
  preserving archived bytes and observation metadata.

## Task 6 — confined filesystem access and ownership preflight

- Actual pre-change regressions showed unsafe SQLite links/shared parents being
  accepted, a sidecar-symlink failure after database mutation, and URI punctuation
  breaking literal filenames. Ownership/path checks now precede SQLite writes,
  migration and permission changes; file URI construction encodes filenames.
- Linux reads are anchored to descriptors, reject all supplied symlink/magic-link
  components and hardlinks, and inspect type before opening for data. Exclusive
  outputs are private and non-executable. Unsupported platforms/filesystems fail
  explicitly, without path-based fallbacks.
- Publication uses no-replace rename, durability checks and post-sync identity
  verification. Regressions cover existing files and empty/nonempty directories,
  late fsync failure, root/file replacement, unsafe ancestors and staged modes,
  FIFO/device refusal, and descriptor leaks. No failure deletes uncertain output.
- Full Go suite PASS (store 82.962s, web 25.001s), vet PASS, and five repeated
  confinement/ownership race runs PASS (safefile 1.466s, store 5.799s). All packages
  and the application cross-build for Linux AMD64, ARM64 and ARMv7. A >2 GiB
  sparse-file characterization passes in a 32-bit test process.
- Read-only production compatibility metadata was inspected; no services, owners,
  configuration or collected data changed. Post-commit verification PASS at
  f29a6e7: safefile 0.044s, store 72.086s. Task 6's local implementation is complete.
  Actual cross-account permission validation remains a Task 16 release gate.

Rulings:

- Use injected ownership metadata locally; perform real cross-account validation
  in Task 16's disposable guest. Cost is a later platform/account integration
  check, which remains mandatory before release.
- Reopen checked O_PATH descriptors through verified kernel procfs, never a
  replaceable supplied name. Cost is requiring Linux procfs; this avoids opening
  devices/FIFOs to discover their type.
- Reject child mount crossings and unverified filesystems. Cost is requiring
  separate configured roots for different supported mounts.
- Add output-namespace validation, including before existing-file adoption.
  Cost is refusal of insecure output layouts; read-only source roots may remain
  owned by the separate collector account.
- Keep test fixtures private through shared test-only setup. Cost is stricter
  fixture defaults, with no production or host-global umask change.
- Verify output-root identity through publication and require protected ancestors.
  Cost is rejecting replaceable output paths; read-only roots retain descriptor
  identity if their original directory is renamed.

## Task 5 — durable archiving and retention integration

- Actual runner/worker regression now archives all 406 inert download events
  across restart, including late timestamps, without changing original bytes.
- Leased/pending jobs and enabled discovery checkpoints hold required source
  data. Disabled discovery releases only undiscovered history, not queued work.
- Capture publication uses private temporary files, verified hashes, no-overwrite
  finalization and fenced durable completion. Existing blobs after a failed
  completion are verified and adopted only when identity is known. Hashless
  missing-source recovery never guesses an association or fetch provenance.
- Raw scans stream directory names, use durable deduplication, and never stamp
  local observations as successful remote fetches. TTY indexing/persistence
  errors are surfaced and retried; transcript derivatives publish atomically.
- Retention preserves note-only actors, pending URL work, shared-file references
  and derivatives. Artifact cleanup uses bounded transactions and requires an
  explicit evidence root before deleting bytes through confined descriptors.
  Outside-root, symlink and active-database targets fail closed. Terminal job
  diagnostics age out only after their events are gone and the outcome ages out.
- Regression failures were observed before fixes, including arbitrary-path
  deletion, the late-page transaction rollback, queued URL deletion, shared
  transcript deletion, hidden TTY failures and overwriting quarantine bytes.
- Full Go suite PASS (store 90.582s, web 24.507s); vet and all-package Linux
  AMD64/ARM64/ARMv7 and Darwin ARM64 builds PASS. Full capture/store/safefile
  race suites PASS (48.457s/249.134s/1.130s). Post-commit checks follow the commit.

Rulings:

- Preserve ambiguous legacy notes, including note-only actors, rather than
  treating annotation text that resembles a generated summary as disposable.
- Raw directory files prove local observation, not remote fetch freshness;
  unverifiable provenance remains ineligible for automatic sharing.
- Harden legacy copy and quarantine publication alongside the new file worker:
  the reproduced overwrite/symlink paths share its evidence namespace.
- Source cleanup recognizes only closed SHA-named files. Unrecognized files are
  conservatively retained instead of assuming permission to delete them.
- Retain paused/stalled URL work until terminal, just as pending file work is
  retained. Cost is visible disk/backlog growth, not silent lost work.
- Empty EvidenceRoot means metadata-only retention; live capture supplies the
  resolved root. Standalone store callers cannot delete arbitrary DB paths.
- Guard only quarantine finalization/recording, keeping network work outside the
  retention critical section. Cost is a narrow internal publication callback.

## Task 7 — consistent non-migrating SQLite snapshots

- New snapshot API uses a dedicated read-only source connection with a pinned
  read transaction and bounded SQLite online-backup steps. Source migrations,
  backfills and raw main/WAL/SHM file copying never run on this path.
- Finalized snapshots are standalone DELETE-journal databases. Verification uses
  a protected descriptor through a read-only VFS, never reopens an untrusted
  checked filename, preserves bytes/directory entries, and does not migrate.
- Fixed-registry counts, SQLite integrity, supported schemas, and bounded
  evidence metadata are checked. Evidence iteration includes file-job result
  pointers and propagates visitor cancellation. Existing destinations refuse
  replacement; interrupted staging remains available rather than being deleted.
- Actual tests cover simultaneous WAL writes/committed prefixes, literal URI
  punctuation, old/future schemas, annotations/settings/ledgers, malformed
  metadata, repeated/concurrent inspection, read-only directories, descriptor
  lifetime, cancellation/admission, busy retries and backup Finish errors.
- The new tests exposed two additional issues: the pinned VFS destructor corrupts
  its allocator, and recent archived jobs could outlive their evidence file.
  Both were reproduced before their fixes. The snapshot/retention interface now
  protects every durable storage reference without inventing fetch freshness.
- Full Go suite PASS (store 83.285s, web 24.196s); three repeated snapshot and
  retention race runs PASS (66.940s); vet and all-package ARM64/ARMv7 builds PASS.

Rulings:

- Keep one process-lifetime read-only VFS registration with at most eight active
  scoped entries. The pinned destructor mismatches sqlite3_malloc/libc.free;
  entries and SQL handles are still released after every operation. Cost is one
  fixed registration until process exit, avoiding an unreviewed driver upgrade.
- Treat file-job result paths as evidence references and keep newly archived
  bytes until result/event retention permits removal. Cost is conservative
  retention for up to one extra maintenance cycle, not fabricated freshness.

## Task 8 — protected backup bundles and read-only verification

- The library creates version-1 directory bundles from a consistent snapshot,
  required evidence and available configured Cowrie sources. Log prefixes have
  explicit lengths/observation times; unrelated log-directory files are excluded.
- Destination overlap/existence, private permissions, stable file identities,
  hashes, counts, schema, source limits and disk space are checked. Extra-file
  inventory excludes DB/sidecar inode aliases before opening their contents.
- Verification is read-only, descriptor-confined and strict about duplicate JSON
  properties/paths, traversal, unsupported types/hardlinks, unknown formats,
  missing/unexpected files, markers, negative sizes and mismatched checksums/counts.
- Real fault tests cover ENOSPC, file/directory fsync failures, a destination
  appearing during publication, rename followed by failed sync, log append versus
  truncate/rotation, evidence replacement and cancellation. Incomplete private
  material remains recoverable and is never reported as a completed backup.
- Reproduced and fixed false completion on errors, lost cancellation identity,
  escaped-name budget undercounting, ambiguous JSON entry properties, and checking
  a different DB from the one whose checksum had been verified.
- Full Go suite PASS (store 202.982s, web 24.960s); backup/safefile/config race
  suites PASS (44.141s/1.143s/1.024s); vet and all-package ARM64/ARMv7 builds PASS.

Rulings:

- Share configuration byte parsing with normal config.Load, preserving identical
  defaults/validation while interpreting exactly the protected copied bytes.
- Bound config parsing to 4 MiB, explicit administrative selections to 256 files,
  and recursive directory depth to 64. These additional safety limits fail
  explicitly rather than silently omitting input; manifest limits remain 64 MiB
  and at most 1,000,000 entries.

## Task 9 — no-overwrite restore and backup CLI

- Added backup create/verify/restore CLI dispatch before normal store/config
  initialization, with strict flags, repeated include-file inputs, positive
  configurable deadlines, dry-run and nonzero incomplete-operation exits.
- Restore verifies first, copies with checksum rechecks into private staging,
  rebases only storage/cursor pointers and preserves raw events, settings,
  annotations and submission ledgers. Original YAML remains metadata; separate
  recovery YAML disables capture/retention without claiming enforced offline
  mode. No service activation or provider call occurs.
- Real round trips preserve three fixture events and exact evidence bytes;
  actual Cowrie replay after cursor rebasing preserves four events without
  duplicates. Legacy v1 remap does not migrate. Existing/overlapping destinations,
  cancellation, late publication failures and invalid destination pointers fail
  safely, leaving incomplete material recoverable.
- Actual regressions reproduced normal configuration being opened during CLI
  verification and explicit long timeouts being silently shortened. Both pass
  after fixes. Supplied triggers on remapped tables are refused before mutation.
- Full Go suite PASS (store 89.293s, web 25.247s); recovery/CLI race checks PASS
  (backup 42.551s, store 17.125s, CLI 4.390s); vet and all-package ARM64/ARMv7 builds
  PASS. Real service-account validation remains the Task 16 integration gate.

Ruling: refuse customized triggers on remapped tables rather than executing
bundle-supplied updates that could rewrite unrelated evidence. Cost is requiring
manual review of such customized snapshots; original material stays intact.

## Task 10 — bounded operational state and metric rendering

- Added fixed enums/arrays for process phases, workers, sources, providers,
  operations, outcomes and failure reasons. Unknown values are rejected without
  allocating labels/state. Snapshots copy state under a mutex; counters reset
  for each new monitor and upstream acceptance is separate from durable sharing.
- Readiness refuses stale/future samples, missing probes, insufficient space,
  dead/erroring/stalled required workers and shutdown. Quiet workers and active
  bounded operations remain healthy; finite backfill completion is distinct
  from failure. Disabled thresholds never disable other health checks.
- Prometheus rendering consumes only the bounded snapshot, performs no I/O
  beyond its supplied writer, propagates write errors, and exposes explicit
  sample/volume availability and enabled state without private labels.
- Clock/worker/cardinality/accounting tests PASS; three repeated race runs PASS
  (2.512s); vet and full Go suite PASS (store 96.943s, web 26.476s).
- Samplers, HTTP routes and runtime instrumentation are still pending, so this
  foundation is not yet advertised as an operational endpoint implementation.

Ruling: return an error from SetPhase to meet the closed-enum validation contract
and reject backwards lifecycle transitions. Existing statement-form calls remain
valid; later lifecycle callers can check the additional result.

## Task 11 — private operational routes and bounded probes

- Added guarded health/readiness/metrics routes with dashboard authentication or
  direct-loopback-only tokenless access, query-token refusal, GET/HEAD/Allow
  handling, bodyless HEAD responses and fixed private status reasons.
- Requests consume monitor snapshots only. Sampling uses five-second cycles and
  one-second budgets; aggregate refresh is independently limited to once a
  minute. Queue/checkpoint/pool results are scalar, cancellable and never queried
  by scrapes. Last-good aggregate age/error/availability stays explicit.
- Filesystem access/free-space probes inspect descriptors without walking or
  modifying evidence. Read-only/missing required volumes fail readiness; disabled
  capture does not require evidence. Min-free defaults to 256 MiB, rejects
  negatives and permits explicit zero to disable only that threshold.
- Actual tests cover auth/methods/HEAD, scrape storms, blocked/cancelled probes,
  pool pressure, injected clocks and read-only/disabled volumes. Full Go suite
  PASS (store 239.666s, web 36.926s), repeated operational race checks PASS, vet
  and all-package ARM64/ARMv7 builds PASS.
- Lifecycle ownership and production monitor/sampler wiring remain Task 12.
  Missing monitors deliberately return unavailable rather than pretending the
  currently unwired daemon is ready.

Ruling: expire optional aggregate availability after two minutes or a failed
refresh, retaining last-good values and sample age. Cost is conservative advisory
unavailability, not false zeros or a failure of otherwise healthy readiness.

## Task 12 — unfinished startup/lifecycle boundary checkpoint

Current uncommitted work adds the lifecycle cancellation/join owner, explicit
listener-binding notification, authenticated startup gates, cancellable journal
subprocess seeding and Cowrie append/rotation context variants. Real tests cover
blocked child cancellation, startup HTTP availability/gating, bind failure and
resource close ordering. The production daemon has not yet been adapted to this
owner; worker/sampler ownership and truthful instrumentation remain unfinished.

The broad checkpoint caught and corrected security-validation ordering for a
pre-cancelled wildcard bind. The real-listener test now owns/drains its HTTP
transport instead of leaving connection cleanup to a global transport. Twenty
repeated startup/wildcard race runs pass. Final checkpoint full Go suite PASS
(store 95.702s, web 27.856s), focused repeated lifecycle/context race checks PASS,
vet and all-package ARM64/ARMv7 builds PASS. These results do not mark Task 12 or
the branch complete. Tasks 13–17 and the independent final review are pending;
no release, Wiki publication or production rollout has occurred.

### Task 12 implementation completion

The production live/web adapters now use the lifecycle owner, wait for a bound
listener before seeding, own/join sampling and ingestion workers, and close the
store only after producers and HTTP handlers/MMDB exit. Real CLI readiness and
SIGTERM tests pass. Worker cycle health distinguishes idle, active, failed and
completed work; bounded journal-summary derivation runs independently from finite
timestamp repairs. Writer admission/append transactions support cancellation.

Observers count committed ingestion, actual provider operations and durable
sharing separately. Tests reproduce and fix missing production readiness wiring
and verify rollback/dedup accounting, upstream acceptance with ledger failure,
dry-run isolation and fixed response outcomes. Full Go suite PASS (store 346.931s,
web 71.399s), full race suite PASS (store 469.083s, web 135.276s), vet and ARM64/
ARMv7 all-package builds PASS. No public provider submission or deployment ran.

Ruling: add a fixed JournalSummaries worker because continuous derivation cannot
share completion state with finite time backfills. Cost is one bounded metric
group, with no dynamic labels. Earlier unfinished checkpoint notes are historical.

## Task 13 — explicit trusted-proxy origins

Public origin and trusted proxy configuration are validated together. Only a
matching direct TCP peer with the preserved external Host may use the configured
HTTPS origin; forwarded headers never grant trust. Cookie bootstrap, rotation
and write-origin checks share that decision. Tokenless proxy peers are denied
operational access even when loopback. Invalid origins, partial configuration,
universal/mapped-universal networks, Host mismatches and spoofed headers are
covered. Full suite PASS (store 91.908s, web 27.580s), repeated proxy/auth/runtime
race checks and vet PASS.

Ruling: centralize parsing in leaf netmatch and expose the planned web API via
an alias, avoiding duplicated config/request security logic and import cycles.

## Task 14 — diagnostic privacy

Actual HTTP/ledger error paths leaked sentinel credentials and paths to logs;
actual VT/enrichment clients returned credential-bearing transport URLs. These
regressions now pass using fixed log categories and the existing safe request
error wrapper, preserving cancellation identity. Capture privacy was already
safe and remains unchanged. Upstream/durable, dry-run and cache-only accounting
checks remain green. Full suite PASS (store 180.870s, web 26.815s), repeated
privacy/accounting race checks and vet PASS.

Ruling: sibling sharing/export log sites use the same safe diagnostic boundary,
trading raw error text for fixed operation and cancellation/deadline categories.

## Task 15 — unfinished installer checkpoint

Real inert-command regressions reproduced broken literal paths in both installer
service renderers. Directive-specific quoting, control-character validation and
account conflict preflight are implemented in the current uncommitted tree.
Unproven Python --purge now refuses before SSH/service mutation, with a tested
directory-identity marker and conservative account retention.

All 95 current Python tests, shell syntax and diff checks pass. This does not
complete Task 15: descriptor-safe ownership changes, comprehensive adoption and
shared provenance rules, generated-unit/YAML validation and real guest permission
coverage remain outstanding. GitHub Actions availability was checked read-only;
no disposable integration run or production change has occurred.
